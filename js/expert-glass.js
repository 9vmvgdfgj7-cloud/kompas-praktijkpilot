window.KompasGlass={
normInfo(zone,lengthMm){const len=Number(lengthMm||0);if(zone==="R")return{limit:null,over:false,text:"Voor zone R geldt voor lijnvormige fouten geen beperking in deze samenvatting."};const limit=zone==="E"?30:15,over=len>limit;return{limit,over,text:`De gemeten lengte bedraagt ${len} mm, waarmee de beschadiging ${over?"groter is dan":"binnen"} de grenswaarde van ${limit} mm die voor zone ${zone} geldt.`}},
qualification(i){if(i.weather!=="Diffuus daglicht / bewolkt"||i.distance==="Nee"||i.zone==="R")return"Constatering";return this.normInfo(i.zone,i.lengthMm).over?"Opleverpunt":"Constatering"},
description(i){const n=this.normInfo(i.zone,i.lengthMm),pos=i.side==="Spouw"?"in de spouw":`aan de ${i.side.toLowerCase()}`,vis=i.distance==="Ja"?"zichtbaar vanaf minimaal 3 meter":"niet zichtbaar vanaf minimaal 3 meter";return `Beschadiging waargenomen aan het isolatieglas in ${i.location.toLowerCase()}. De beschadiging bevindt zich ${pos}, in zone ${i.zone}, is ${vis} en is geconstateerd bij ${i.weather.toLowerCase()}. ${n.text}`},
reasoning(i){const n=this.normInfo(i.zone,i.lengthMm),a=["Toetsingskader: beoordeling van meerbladig isolatieglas bij oplevering.","Beoordeling op doorzicht, zonder vooraf markeren, vanaf minimaal 3 meter, zo loodrecht mogelijk en bij diffuus daglicht.",n.text];if(i.weather!=="Diffuus daglicht / bewolkt")a.push("De lichtomstandigheden wijken af van de beoordelingsmethode.");if(i.distance==="Nee")a.push("De beschadiging is niet zichtbaar vanaf de minimale beoordelingsafstand.");return a.join("\n")}
};